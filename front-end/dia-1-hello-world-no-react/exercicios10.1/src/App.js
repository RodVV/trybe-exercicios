import './App.css';
import Header from './components/Header';

const array = ['acordar', 'comer', 'estudar', 'trabalhar', 'dormir'];

// const Task = (value) => {  
//     return (
//       <li>{value}</li>
//       );
// }

function App() {
  return (< Header />);
}

export default App;

/* <ul>{ array.map((item) => <li>{ Task(item) } </li>) }</ul> */
